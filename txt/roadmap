- fix yaml env variable

- Add a source manager dialog
    Allows to add/remove/silent scripts source folders from the config and their related scripts

- Shotcuts Widget to display the available shortcuts of the tool

- Ability to switch the visible tools by updating the ui when switching from an application to another

- Ability to add favorite tools and put them in beginning of the tools list (Double click on a tool button)

- Create a cache file to store the latest state of the ui with a save and load button to load external cache files
